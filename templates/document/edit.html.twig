{% extends 'base.html.twig' %}

{% block title %}Modifier un document{% endblock %}

{% block page %}

<div class="stages-section justify-content-center">
    <div class="container">
        <div class="row">
            <div class="col-lg-6 mx-auto mb-5 text-center">
                <h1>Administration</h1>
                <h2 class="text-primary mb-5">Modifier un document</h2>

                {{ include('document/_form.html.twig', {'button_label': 'Modifier'}) }}

                <a href="{{ path('document_index') }}">Retour</a>
            </div>
        </div>
    </div>
</div>

{% endblock %}
