{% extends 'base.html.twig' %}

{% block title %}Galerie-ajout-{% endblock %}

{% block page %}

<section id="gallery" class="gallery-section">
    <div class="container">
        <div class="col-lg-8 mx-auto mb-5 text-center">
            <h1 class="mb-5">Galerie</h1>
            <h2 class="text-primary">Editer une image</h2>
        </div>
        <div class="row">
            <div class="col-lg-8 mx-auto mb-5 text-center">
                
                        {{ include('gallery/_form.html.twig') }}
                   
            </div>
        </div>
    </div>
</section>

{% endblock %}
